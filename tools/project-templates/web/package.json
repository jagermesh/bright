{
  "name": "bright-site",
  "description": "Bright Site",
  "version": "1.0.0",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-phplint": "^0.9.0",
    "gulp-rename": "^1.4.0",
    "gulp-sass-universal": "^1.0.1",
    "gulp-shell": "^0.7.1",
    "gulp-terser": "^1.3.2",
    "gulp-eslint": "^6.0.0",
    "eslint": "^8.7.0",
    "merge-stream": "^2.0.0",
    "phplint": "^2.0.3",
    "sass": "^1.26.10",
    "terser": "^4.8.0"
  },
  "scripts": {
    "build": "composer install && cd ./vendor/jagermesh/bright && npm run build && cd ../../../ && npm install && gulp build",
    "update": "composer update && cd ./vendor/jagermesh/bright && npm run update && cd ../../../ && npm update && gulp build"
  }
}
