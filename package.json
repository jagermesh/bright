{
  "name": "bright",
  "description": "Reactive PHP/JavaScript Framework",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-jshint": "^2.1.0",
    "gulp-phplint": "^0.9.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass-universal": "^1.0.1",
    "gulp-shell": "^0.8.0",
    "gulp-terser": "^2.0.1",
    "jshint": "^2.12.0",
    "merge-stream": "^2.0.0",
    "phplint": "^2.0.5",
    "sass": "^1.33.0",
    "terser": "^5.7.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/jagermesh/bright.git"
  },
  "scripts": {
    "build": "npm install && gulp build",
    "update": "npm update && gulp build",
    "test": "npm install && gulp build && composer install && gulp test"
  }
}
